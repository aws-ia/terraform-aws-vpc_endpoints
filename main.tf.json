{
  "resource": {
    "aws_vpc_endpoint": {
      "sagemaker_notebook_interface": {
        "count": "${var.sagemaker_notebook_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"aws.sagemaker.<REGION>.notebook\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.sagemaker_notebook_interface_policy}"
      },
      "sagemaker_studio_interface": {
        "count": "${var.sagemaker_studio_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"aws.sagemaker.<REGION>.studio\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.sagemaker_studio_interface_policy}"
      },
      "s3-global_accesspoint_interface": {
        "count": "${var.s3-global_accesspoint_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.s3-global.accesspoint\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.s3-global_accesspoint_interface_policy}"
      },
      "access-analyzer_interface": {
        "count": "${var.access-analyzer_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.access-analyzer\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.access-analyzer_interface_policy}"
      },
      "acm-pca_interface": {
        "count": "${var.acm-pca_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.acm-pca\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.acm-pca_interface_policy}"
      },
      "airflow_api_interface": {
        "count": "${var.airflow_api_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.airflow.api\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.airflow_api_interface_policy}"
      },
      "airflow_env_interface": {
        "count": "${var.airflow_env_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.airflow.env\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.airflow_env_interface_policy}"
      },
      "airflow_ops_interface": {
        "count": "${var.airflow_ops_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.airflow.ops\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.airflow_ops_interface_policy}"
      },
      "app-integrations_interface": {
        "count": "${var.app-integrations_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.app-integrations\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.app-integrations_interface_policy}"
      },
      "application-autoscaling_interface": {
        "count": "${var.application-autoscaling_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.application-autoscaling\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.application-autoscaling_interface_policy}"
      },
      "appmesh-envoy-management_interface": {
        "count": "${var.appmesh-envoy-management_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.appmesh-envoy-management\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "apprunner_interface": {
        "count": "${var.apprunner_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.apprunner\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.apprunner_interface_policy}"
      },
      "appstream_api_interface": {
        "count": "${var.appstream_api_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.appstream.api\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "appstream_streaming_interface": {
        "count": "${var.appstream_streaming_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.appstream.streaming\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "aps-workspaces_interface": {
        "count": "${var.aps-workspaces_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.aps-workspaces\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "athena_interface": {
        "count": "${var.athena_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.athena\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.athena_interface_policy}"
      },
      "auditmanager_interface": {
        "count": "${var.auditmanager_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.auditmanager\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.auditmanager_interface_policy}"
      },
      "autoscaling_interface": {
        "count": "${var.autoscaling_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.autoscaling\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.autoscaling_interface_policy}"
      },
      "autoscaling-plans_interface": {
        "count": "${var.autoscaling-plans_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.autoscaling-plans\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.autoscaling-plans_interface_policy}"
      },
      "awsconnector_interface": {
        "count": "${var.awsconnector_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.awsconnector\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "braket_interface": {
        "count": "${var.braket_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.braket\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.braket_interface_policy}"
      },
      "cassandra_interface": {
        "count": "${var.cassandra_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.cassandra\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.cassandra_interface_policy}"
      },
      "cassandra-fips_interface": {
        "count": "${var.cassandra-fips_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.cassandra-fips\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.cassandra-fips_interface_policy}"
      },
      "clouddirectory_interface": {
        "count": "${var.clouddirectory_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.clouddirectory\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.clouddirectory_interface_policy}"
      },
      "cloudformation_interface": {
        "count": "${var.cloudformation_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.cloudformation\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "cloudhsmv2_interface": {
        "count": "${var.cloudhsmv2_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.cloudhsmv2\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.cloudhsmv2_interface_policy}"
      },
      "cloudtrail_interface": {
        "count": "${var.cloudtrail_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.cloudtrail\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "codeartifact_api_interface": {
        "count": "${var.codeartifact_api_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.codeartifact.api\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.codeartifact_api_interface_policy}"
      },
      "codeartifact_repositories_interface": {
        "count": "${var.codeartifact_repositories_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.codeartifact.repositories\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.codeartifact_repositories_interface_policy}"
      },
      "codebuild_interface": {
        "count": "${var.codebuild_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.codebuild\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.codebuild_interface_policy}"
      },
      "codebuild-fips_interface": {
        "count": "${var.codebuild-fips_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.codebuild-fips\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.codebuild-fips_interface_policy}"
      },
      "codecommit_interface": {
        "count": "${var.codecommit_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.codecommit\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.codecommit_interface_policy}"
      },
      "codecommit-fips_interface": {
        "count": "${var.codecommit-fips_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.codecommit-fips\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.codecommit-fips_interface_policy}"
      },
      "codedeploy_interface": {
        "count": "${var.codedeploy_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.codedeploy\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.codedeploy_interface_policy}"
      },
      "codedeploy-commands-secure_interface": {
        "count": "${var.codedeploy-commands-secure_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.codedeploy-commands-secure\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.codedeploy-commands-secure_interface_policy}"
      },
      "codeguru-profiler_interface": {
        "count": "${var.codeguru-profiler_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.codeguru-profiler\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "codeguru-reviewer_interface": {
        "count": "${var.codeguru-reviewer_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.codeguru-reviewer\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "codepipeline_interface": {
        "count": "${var.codepipeline_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.codepipeline\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "codestar-connections_api_interface": {
        "count": "${var.codestar-connections_api_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.codestar-connections.api\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.codestar-connections_api_interface_policy}"
      },
      "comprehend_interface": {
        "count": "${var.comprehend_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.comprehend\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.comprehend_interface_policy}"
      },
      "comprehendmedical_interface": {
        "count": "${var.comprehendmedical_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.comprehendmedical\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.comprehendmedical_interface_policy}"
      },
      "config_interface": {
        "count": "${var.config_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.config\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.config_interface_policy}"
      },
      "databrew_interface": {
        "count": "${var.databrew_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.databrew\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.databrew_interface_policy}"
      },
      "dataexchange_interface": {
        "count": "${var.dataexchange_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.dataexchange\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.dataexchange_interface_policy}"
      },
      "datasync_interface": {
        "count": "${var.datasync_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.datasync\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "devops-guru_interface": {
        "count": "${var.devops-guru_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.devops-guru\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.devops-guru_interface_policy}"
      },
      "dms_interface": {
        "count": "${var.dms_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.dms\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.dms_interface_policy}"
      },
      "dms-fips_interface": {
        "count": "${var.dms-fips_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.dms-fips\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.dms-fips_interface_policy}"
      },
      "ebs_interface": {
        "count": "${var.ebs_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.ebs\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "ec2_interface": {
        "count": "${var.ec2_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.ec2\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.ec2_interface_policy}"
      },
      "ec2messages_interface": {
        "count": "${var.ec2messages_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.ec2messages\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.ec2messages_interface_policy}"
      },
      "ecr_api_interface": {
        "count": "${var.ecr_api_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.ecr.api\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.ecr_api_interface_policy}"
      },
      "ecr_dkr_interface": {
        "count": "${var.ecr_dkr_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.ecr.dkr\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.ecr_dkr_interface_policy}"
      },
      "ecs_interface": {
        "count": "${var.ecs_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.ecs\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.ecs_interface_policy}"
      },
      "ecs-agent_interface": {
        "count": "${var.ecs-agent_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.ecs-agent\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.ecs-agent_interface_policy}"
      },
      "ecs-telemetry_interface": {
        "count": "${var.ecs-telemetry_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.ecs-telemetry\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.ecs-telemetry_interface_policy}"
      },
      "elastic-inference_runtime_interface": {
        "count": "${var.elastic-inference_runtime_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.elastic-inference.runtime\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "elasticbeanstalk_interface": {
        "count": "${var.elasticbeanstalk_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.elasticbeanstalk\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.elasticbeanstalk_interface_policy}"
      },
      "elasticbeanstalk-health_interface": {
        "count": "${var.elasticbeanstalk-health_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.elasticbeanstalk-health\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.elasticbeanstalk-health_interface_policy}"
      },
      "elasticfilesystem_interface": {
        "count": "${var.elasticfilesystem_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.elasticfilesystem\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.elasticfilesystem_interface_policy}"
      },
      "elasticfilesystem-fips_interface": {
        "count": "${var.elasticfilesystem-fips_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.elasticfilesystem-fips\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.elasticfilesystem-fips_interface_policy}"
      },
      "elasticloadbalancing_interface": {
        "count": "${var.elasticloadbalancing_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.elasticloadbalancing\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.elasticloadbalancing_interface_policy}"
      },
      "elasticmapreduce_interface": {
        "count": "${var.elasticmapreduce_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.elasticmapreduce\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.elasticmapreduce_interface_policy}"
      },
      "email-smtp_interface": {
        "count": "${var.email-smtp_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.email-smtp\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "emr-containers_interface": {
        "count": "${var.emr-containers_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.emr-containers\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.emr-containers_interface_policy}"
      },
      "events_interface": {
        "count": "${var.events_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.events\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.events_interface_policy}"
      },
      "execute-api_interface": {
        "count": "${var.execute-api_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.execute-api\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.execute-api_interface_policy}"
      },
      "finspace_interface": {
        "count": "${var.finspace_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.finspace\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.finspace_interface_policy}"
      },
      "finspace-api_interface": {
        "count": "${var.finspace-api_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.finspace-api\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.finspace-api_interface_policy}"
      },
      "fis_interface": {
        "count": "${var.fis_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.fis\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.fis_interface_policy}"
      },
      "frauddetector_interface": {
        "count": "${var.frauddetector_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.frauddetector\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.frauddetector_interface_policy}"
      },
      "git-codecommit_interface": {
        "count": "${var.git-codecommit_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.git-codecommit\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.git-codecommit_interface_policy}"
      },
      "git-codecommit-fips_interface": {
        "count": "${var.git-codecommit-fips_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.git-codecommit-fips\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.git-codecommit-fips_interface_policy}"
      },
      "glue_interface": {
        "count": "${var.glue_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.glue\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.glue_interface_policy}"
      },
      "greengrass_interface": {
        "count": "${var.greengrass_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.greengrass\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.greengrass_interface_policy}"
      },
      "groundstation_interface": {
        "count": "${var.groundstation_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.groundstation\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.groundstation_interface_policy}"
      },
      "healthlake_interface": {
        "count": "${var.healthlake_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.healthlake\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.healthlake_interface_policy}"
      },
      "imagebuilder_interface": {
        "count": "${var.imagebuilder_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.imagebuilder\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.imagebuilder_interface_policy}"
      },
      "iot_data_interface": {
        "count": "${var.iot_data_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.iot.data\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "iotsitewise_api_interface": {
        "count": "${var.iotsitewise_api_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.iotsitewise.api\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.iotsitewise_api_interface_policy}"
      },
      "iotsitewise_data_interface": {
        "count": "${var.iotsitewise_data_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.iotsitewise.data\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.iotsitewise_data_interface_policy}"
      },
      "iotwireless_api_interface": {
        "count": "${var.iotwireless_api_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.iotwireless.api\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "kendra_interface": {
        "count": "${var.kendra_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.kendra\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.kendra_interface_policy}"
      },
      "kinesis-firehose_interface": {
        "count": "${var.kinesis-firehose_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.kinesis-firehose\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.kinesis-firehose_interface_policy}"
      },
      "kinesis-streams_interface": {
        "count": "${var.kinesis-streams_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.kinesis-streams\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.kinesis-streams_interface_policy}"
      },
      "kms_interface": {
        "count": "${var.kms_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.kms\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.kms_interface_policy}"
      },
      "lambda_interface": {
        "count": "${var.lambda_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.lambda\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.lambda_interface_policy}"
      },
      "license-manager_interface": {
        "count": "${var.license-manager_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.license-manager\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.license-manager_interface_policy}"
      },
      "license-manager-fips_interface": {
        "count": "${var.license-manager-fips_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.license-manager-fips\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.license-manager-fips_interface_policy}"
      },
      "logs_interface": {
        "count": "${var.logs_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.logs\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.logs_interface_policy}"
      },
      "lookoutequipment_interface": {
        "count": "${var.lookoutequipment_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.lookoutequipment\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.lookoutequipment_interface_policy}"
      },
      "lookoutmetrics_interface": {
        "count": "${var.lookoutmetrics_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.lookoutmetrics\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.lookoutmetrics_interface_policy}"
      },
      "lookoutvision_interface": {
        "count": "${var.lookoutvision_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.lookoutvision\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.lookoutvision_interface_policy}"
      },
      "lorawan_cups_interface": {
        "count": "${var.lorawan_cups_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.lorawan.cups\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "lorawan_lns_interface": {
        "count": "${var.lorawan_lns_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.lorawan.lns\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "macie2_interface": {
        "count": "${var.macie2_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.macie2\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.macie2_interface_policy}"
      },
      "mgn_interface": {
        "count": "${var.mgn_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.mgn\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.mgn_interface_policy}"
      },
      "monitoring_interface": {
        "count": "${var.monitoring_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.monitoring\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.monitoring_interface_policy}"
      },
      "nimble_interface": {
        "count": "${var.nimble_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.nimble\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.nimble_interface_policy}"
      },
      "profile_interface": {
        "count": "${var.profile_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.profile\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.profile_interface_policy}"
      },
      "proton_interface": {
        "count": "${var.proton_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.proton\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.proton_interface_policy}"
      },
      "qldb_session_interface": {
        "count": "${var.qldb_session_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.qldb.session\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.qldb_session_interface_policy}"
      },
      "rds_interface": {
        "count": "${var.rds_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.rds\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.rds_interface_policy}"
      },
      "rds-data_interface": {
        "count": "${var.rds-data_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.rds-data\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.rds-data_interface_policy}"
      },
      "redshift_interface": {
        "count": "${var.redshift_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.redshift\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.redshift_interface_policy}"
      },
      "redshift-data_interface": {
        "count": "${var.redshift-data_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.redshift-data\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "redshift-fips_interface": {
        "count": "${var.redshift-fips_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.redshift-fips\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.redshift-fips_interface_policy}"
      },
      "rekognition_interface": {
        "count": "${var.rekognition_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.rekognition\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.rekognition_interface_policy}"
      },
      "rekognition-fips_interface": {
        "count": "${var.rekognition-fips_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.rekognition-fips\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.rekognition-fips_interface_policy}"
      },
      "s3_interface": {
        "count": "${var.s3_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.s3\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.s3_interface_policy}"
      },
      "sagemaker_api_interface": {
        "count": "${var.sagemaker_api_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.sagemaker.api\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.sagemaker_api_interface_policy}"
      },
      "sagemaker_featurestore-runtime_interface": {
        "count": "${var.sagemaker_featurestore-runtime_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.sagemaker.featurestore-runtime\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.sagemaker_featurestore-runtime_interface_policy}"
      },
      "sagemaker_runtime_interface": {
        "count": "${var.sagemaker_runtime_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.sagemaker.runtime\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.sagemaker_runtime_interface_policy}"
      },
      "sagemaker_runtime-fips_interface": {
        "count": "${var.sagemaker_runtime-fips_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.sagemaker.runtime-fips\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "secretsmanager_interface": {
        "count": "${var.secretsmanager_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.secretsmanager\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.secretsmanager_interface_policy}"
      },
      "servicecatalog_interface": {
        "count": "${var.servicecatalog_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.servicecatalog\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "servicecatalog-appregistry_interface": {
        "count": "${var.servicecatalog-appregistry_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.servicecatalog-appregistry\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.servicecatalog-appregistry_interface_policy}"
      },
      "sms_interface": {
        "count": "${var.sms_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.sms\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "sms-fips_interface": {
        "count": "${var.sms-fips_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.sms-fips\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "snow-device-management_interface": {
        "count": "${var.snow-device-management_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.snow-device-management\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.snow-device-management_interface_policy}"
      },
      "sns_interface": {
        "count": "${var.sns_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.sns\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.sns_interface_policy}"
      },
      "sqs_interface": {
        "count": "${var.sqs_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.sqs\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.sqs_interface_policy}"
      },
      "ssm_interface": {
        "count": "${var.ssm_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.ssm\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.ssm_interface_policy}"
      },
      "ssmmessages_interface": {
        "count": "${var.ssmmessages_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.ssmmessages\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.ssmmessages_interface_policy}"
      },
      "states_interface": {
        "count": "${var.states_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.states\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.states_interface_policy}"
      },
      "storagegateway_interface": {
        "count": "${var.storagegateway_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.storagegateway\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "sts_interface": {
        "count": "${var.sts_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.sts\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.sts_interface_policy}"
      },
      "synthetics_interface": {
        "count": "${var.synthetics_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.synthetics\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.synthetics_interface_policy}"
      },
      "textract_interface": {
        "count": "${var.textract_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.textract\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.textract_interface_policy}"
      },
      "textract-fips_interface": {
        "count": "${var.textract-fips_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.textract-fips\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.textract-fips_interface_policy}"
      },
      "transcribe_interface": {
        "count": "${var.transcribe_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.transcribe\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.transcribe_interface_policy}"
      },
      "transcribestreaming_interface": {
        "count": "${var.transcribestreaming_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.transcribestreaming\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.transcribestreaming_interface_policy}"
      },
      "transfer_interface": {
        "count": "${var.transfer_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.transfer\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "transfer_server_interface": {
        "count": "${var.transfer_server_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.transfer.server\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "workspaces_interface": {
        "count": "${var.workspaces_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.workspaces\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.workspaces_interface_policy}"
      },
      "xray_interface": {
        "count": "${var.xray_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.xray\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}",
        "policy": "${var.xray_interface_policy}"
      },
      "vpce_us-east-1_vpce-svc-0d5a9851843fed971_interface": {
        "count": "${var.vpce_us-east-1_vpce-svc-0d5a9851843fed971_interface_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.vpce.<REGION>.vpce-svc-0d5a9851843fed971\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Interface",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "security_group_ids": "${var.security_group_ids}",
        "subnet_ids": "${length(var.subnet_ids) > 0 ? var.subnet_ids : null}"
      },
      "dynamodb_gateway": {
        "count": "${var.dynamodb_gateway_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.dynamodb\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Gateway",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "route_table_ids": "${length(var.route_table_ids) > 0 ? var.route_table_ids : null}",
        "policy": "${var.dynamodb_gateway_policy}"
      },
      "s3_gateway": {
        "count": "${var.s3_gateway_enabled ? 1 : 0}",
        "service_name": "${replace(\"com.amazonaws.<REGION>.s3\", \"<REGION>\", data.aws_region.current.name)}",
        "vpc_endpoint_type": "Gateway",
        "tags": "${var.tags}",
        "auto_accept": true,
        "vpc_id": "${var.vpc_id}",
        "route_table_ids": "${length(var.route_table_ids) > 0 ? var.route_table_ids : null}",
        "policy": "${var.s3_gateway_policy}"
      }
    }
  }
}